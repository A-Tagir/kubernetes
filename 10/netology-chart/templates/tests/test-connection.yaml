apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netology-chart.fullname" . }}-test-connection"
  labels:
    {{- include "netology-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netology-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
