apiVersion: v1
kind: Service
metadata:
  name: {{ include "netology-chart.fullname" . }}
  labels:
    {{- include "netology-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netology-chart.selectorLabels" . | nindent 4 }}
